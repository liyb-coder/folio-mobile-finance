# Folio 开发路线图

> 当前分期范围、退出标准与准备清单见 [`docs/02_PRODUCT_ROADMAP.md`](docs/02_PRODUCT_ROADMAP.md)；本文保留为前期里程碑记录。

> 基线日期：2026-07-24  
> 原则：每个里程碑以可验证证据结束；界面完成不等于数据和安全完成。

## 当前 MVP 边界

当前 MVP 只交付用户本人 Apple Silicon Mac 上可长期使用的本地应用。移动端、私人 Web、生产云同步、外部 AI/转写、飞书 API 和银行直连全部移到 MVP 之后；已经完成的同步协议继续保留，但不作为 MVP 发布门。

Mac MVP 发布门：

- [x] SQLCipher 保险库、应用密码、自动锁定和 Touch ID 原生链路已实现。
- [x] 账户、持仓、流水、事项、规划、通知、设备内文件识别和本地 AI 核对链已实现。
- [x] CSV/TSV/XLSX、显式表格粘贴、加密备份恢复和可移植导出已实现。
- [x] 最新 DMG 已通过只读挂载、arm64/Bundle/最低系统、签名/权限、动态库路径和私有文件排除自动烟测。
- [x] `macos:mvp:doctor` 已结构化区分自动门、本人自用手工验收与对外分发门；当前自动门 4/4 通过，8 项手工验收待记录。
- [ ] 在用户 Mac 完成 Touch ID 登记/解锁、密码回退、重启后解锁的交互验收。
- [ ] 用代表性真实数据副本完成导入映射、余额对账、重复导入、备份恢复和导出可读性验收。
- [ ] 用最新 DMG 完成干净安装、保留数据升级、卸载不误删用户保险库和异常中断恢复验收。
- [ ] 若要无 Gatekeeper 警告地分发给其他用户，安装完整 Xcode，配置有效 Developer ID，并完成签名、公证和 stapling。

## M0 · 产品与隐私基线

目标：让仓库可以安全公开，并明确产品、安全和发布边界。

- [x] 产品需求、技术设计和安全模型形成开发基线。
- [x] 添加 Git 隐私忽略规则和公开环境变量模板。
- [x] 添加仓库隐私自动测试。
- [x] 统一演示、本地、同步三种数据模式的运行时配置。

验收证据：

- 文档相互一致，提醒页主操作为“添加事项”。
- `.env`、本地数据库、私有导入和备份均被 Git 忽略。
- 测试能拒绝前端 service role/secret key 和常见私有文件。

## M1 · 可替换数据层

目标：从页面中移出演示财务数据，为真实本地仓库留下稳定接口。

- [x] 建立 `DataRepository` 契约和数据模式工厂。
- [x] 将虚构数据迁移到独立 demo adapter。
- [x] 页面只通过 repository 获取数据，不直接定义账户、流水和事项。
- [x] 添加 repository 契约测试和仓库隐私测试。

验收证据：

- 切换适配器不需要修改页面组件。
- demo 数据不含 React 图标或平台 API。
- `npm run build` 和全部自动测试通过。

## M2 · macOS 本地可用版

目标：形成可安装、可解锁、可导入真实数据的 Mac 应用。

- [x] 初始化 Tauri 2，建立严格命令白名单并生成品牌应用图标。
- [x] 建立整数金额、追加式账本、草稿确认和幂等导入领域核心。
- [x] 建立不可变 SQLite schema 和自动约束测试。
- [x] 建立应用锁状态机与原生保险库命令边界。
- [x] 构建并启动 Apple Silicon `Folio.app` 调试包。
- [x] 接入 SQLCipher 加密 SQLite、schema migration 和密文静态检查。
- [x] 实现随机 DEK、Argon2id 密钥封装、保险库创建和应用密码解锁。
- [x] 接入 macOS Data Protection Keychain、LocalAuthentication 能力检查和当前指纹集访问控制。
- [x] 接通 React 保险库创建、应用密码/Touch ID 解锁、本地快照和安全状态界面。
- [x] 补齐安全设置中的 Touch ID 后置启用/关闭：解锁态、密码重新认证、明确确认和 Keychain 失败关闭。
- [x] 将窗口失焦、应用后台和 5 分钟空闲自动锁定接到原生会话，并卸载敏感页面状态。
- [x] 完成手动新增账户的草稿核对、明确确认、期初余额事件、审计记录和加密快照刷新。
- [x] 完成账户资料编辑、并发变化拒绝、零余额安全归档和历史保留。
- [x] 完成账户内持仓/产品明细、状态筛选、表现展示、资料编辑、安全归档和不可变估值快照；持仓市值明确不重复计入账户余额或净资产。
- [x] 阻止仍有有效持仓的账户被归档，避免产品挂在已归档账户下；必须先归档账户内持仓。
- [x] 完成手动收入、支出和同币种双边转账的草稿核对、原子确认、幂等、余额守恒与审计。
- [x] 完成财务事项新增、编辑、完成、归档的草稿核对、并发拒绝、幂等与审计。
- [x] 完成隐私安全的本机系统提醒：明确授权、通用/标题模式、稳定去重、权限撤销失败关闭和自动重排。
- [x] 完成正式流水冲销/修订：反向事件、原子替代、并发复核、幂等确认与历史保留。
- [ ] 在用户设备完成交互式 Touch ID 登记和解锁真机验收。
- [x] 完成 CSV/XLSX 导入、预览、确认、幂等和对账报告。
- [x] 完成加密备份与恢复。
- [x] 完成可移植明文数据导出：标准 CSV/ZIP、可选审计日志、重新认证、明确风险确认、原子写入和成功审计。

验收证据：

- `.app` 在干净测试账号上安装并离线运行。
- 数据库、WAL 和备份静态检查无财务明文。
- 重复导入不增加余额；锁定后内存和界面不再显示敏感数据。
- 备份可恢复，错误密码和篡改备份被拒绝。
- 用户可以不依赖 Folio 导出和核查自己的数据；明文导出不会与加密备份混淆。

## M3 · 身份与可选同步

目标：支持安全的 Web 访问和多设备同步，同时保留纯本地模式。

- [ ] Supabase Auth 密码登录。
  - 已实现仅在 `sync` 模式启用的 Web 登录/注册门，以及 macOS 安全设置中的原生身份登录/注册入口、通用错误提示和内存会话；待连接 Folio 专属 Supabase 项目完成真实邮件与会话验收。
- [ ] 通行密钥在功能开关后接入并完成兼容测试。
  - 已锁定 `@supabase/supabase-js 2.105.0` 并接入实验功能开关；本机安全来源的入口与登记 UI 已验证，待生产 RP ID/HTTPS 域名确定后完成真实 WebAuthn ceremony。
- [ ] 建立 vault membership、RLS 和跨用户否定测试。
  - 已提交全表 RLS/强制 RLS、最小授权和 pgTAP 跨用户测试；当前开发机无 Docker，且尚未创建 Folio 云项目，因此数据库级测试仍需在 CI 或专属项目执行。
- [ ] 私有附件存储与短期签名 URL。
  - 已建立 10 MB 私有 bucket、用户目录与活跃 membership 策略；短期签名 URL 客户端尚未接线。
- [ ] 账本事件增量同步、设备列表和冲突核对。
  - 已建立账户、持仓资料、估值、账本、产品操作、补偿更正和提醒的追加式密文领域事件 schema、AAD v2、逻辑时钟、设备哈希链、幂等键、设备/包装密钥与追加式冲突。
  - SQLCipher 原生层已完成不可变 outbox/inbox、远端事件哈希与认证解密、完整依赖顺序应用、游标推进和跨设备分歧隔离；自动化第二设备已从密文重建持仓全历史并通过重复回放、缺依赖零部分写入及并发冲突测试。
  - macOS 安全设置已接入“身份登录 → 明确同意启用 → 立即同步 → 状态/冲突计数 → 明确停用 → 退出会话”闭环；远端首次初始化失败会自动回滚为关闭，默认仍为纯本地。
  - 本机冲突核对已支持重新验签/认证解密、只显示必要领域字段、明确保留本机、不可变解决记录和审计历史；不会开放绕过领域草稿确认的“直接采用远端”。
  - 已登记设备提供身份与 vault 双重限定的最小只读列表；设备撤销仍等待近期重新认证、同步密钥轮换与其他设备 rewrap 设计。
  - 待 Folio 专属 Supabase 项目执行真实断网重放、RLS 下载、冲突核对和安全设备撤销演练后勾选。
- [x] 明确并实现客户端加密字段。
  - 账户、金额、备注、标签和附件元数据仅进入 XChaCha20-Poly1305 密文；云端只保留 UUID、逻辑时钟、幂等键、哈希、密文长度和必要时间元数据。

验收证据：

- 用户 A 无法访问用户 B 的任何行或文件。
- 断网修改可在恢复网络后幂等同步。
- 关闭同步不会影响本地账本使用。
- Web 端在 HTTPS 下支持密码或通行密钥登录及重新认证。

## M4 · AI 与真实数据采集

目标：让 AI 安全地降低录入和理解成本。

- [ ] 语音转写、图片/PDF 解析和结构化抽取。
  - 已完成文本录入和能力受限的系统语音识别入口；系统语音可能联网，必须逐次明确同意，Folio 不保存原始音频。
  - 已完成 Apple 设备端 PNG/JPEG/HEIC/TIFF OCR，以及 PDF 可选文字提取与无文字层页面逐页 OCR；原件和路径不保存，字段证据可追溯到页码、文字范围、置信度和图片坐标。
  - 已完成账户、收入/支出/转账和财务事项的中文本地规则抽取；独立云端/离线语音转写尚未接入。
  - 已完成规划安全垫与六类配置目标抽取；沿用值也会在最终核对页完整展示。
- [ ] `ModelProvider` 支持云模型与本地模型。
  - 已落地统一能力注册表、`folio_local_v1` 本地提供器和版本化解析器；外部提供器在每次调用未显式允许传输时默认拒绝。
  - 云模型、本地推理模型与用户密钥配置仍待实现。
- [x] 当前结构化 AI 结果只进入草稿核对页，展示原文、字段证据、置信度、不确定项和原生层最终复核。
  - 原生层只接受已登记的本地提供器，并要求绑定同类型 `needs_review` 领域草稿；AI 提案自身不能确认草稿或写账本。
- [x] AI 问答引用账本事件和数据更新时间。
  - `local_ledger_qa_v2` 支持账户/总余额、今日/本月收支、最近流水、待处理事项、本月与上月比较、支出分类、金额最大支出和现金流解释。
  - 聚合结论显示来源总数、当前展示引用数、账本事件 ID、数据时间和计算时间；冲销、转账和其他币种按问题语义明确排除，不调用生成模型猜测原因。
- [ ] 飞书 Sheet/Base 只读导入与增量幂等。

验收证据：

- 未确认的模型输出不影响任何正式汇总。
- 低置信度金额、账户、日期和动作无法跳过核对。
- 关闭云模型后，真实文件不会发送到外部模型服务。

## M5 · iOS 与 Android

目标：在移动端安全查看、语音补充和处理事项。

- [ ] Tauri iOS/Android 工程和平台 CI。
  - 已确认共享 Rust crate 使用 `tauri::mobile_entry_point`，补齐可重复初始化命令和 `npm run mobile:doctor`。
  - 当前 Mac 已安装全部 7 个移动 Rust targets，但缺少完整 Xcode/iPhone SDK、CocoaPods、JDK 和 Android SDK/NDK，因此未生成不可编译的原生空工程。
- [ ] Face ID/Touch ID/BiometricPrompt 解锁。
- [ ] 底部导航中央语音入口和移动端草稿核对。
  - 共享 React 工作区已完成五栏固定底部导航、中央语音入口、安全区适配和全模块抽屉；待 iOS/Android WebView 与真机验收后勾选。
- [ ] 本地加密缓存、离线队列和后台同步。
- [ ] 推送提醒的幂等、去重和隐私预览设置。

验收证据：

- 真机完成安装、升级、锁定、离线记录和同步测试。
- 通知默认不暴露金额或账户名称。
- 丢失设备可撤销会话且不影响其他设备账本。

## M6 · 发布加固

目标：具备可长期使用和安全升级的正式版本。

- [ ] macOS 签名、公证、自动更新签名。
- [ ] iOS TestFlight / App Store 与 Android 签名发布流程。
- [ ] CSP、依赖、Tauri 命令、日志和密钥轮换审计。
- [ ] 性能、无障碍、迁移、崩溃恢复和隐私政策。
- [ ] 完成威胁演练和恢复演练。

验收证据：

- 发布物可追溯到 Git commit、schema、SBOM 和签名身份。
- 升级失败可安全回滚，不损坏账本。
- 安全发布门全部通过并记录结果。

## 当前执行顺序

1. M0 与 M1 已完成。
2. 当前只收口 Mac MVP：Touch ID/密码真机验收、代表性真实数据演练、最新 DMG 干净安装/升级/恢复。
   - 本地数据导入、加密备份恢复、标准 CSV 可移植导出、macOS 本机事项通知和本地 AI 已完成实现。
   - 用户本人内部使用不等待 Supabase、移动端或外部模型；对外无警告分发才需要 Developer ID 签名与公证。
3. M3 已完成的本地加密同步协议、冲突保留本机决议和最小设备状态保留为后续能力；真实 Supabase、RLS、网络和设备撤销演练移出 MVP。
4. M4 已完成 Apple 设备内图片/PDF 证据链与跨期可追溯账本问答；可配置转写/模型提供器和飞书 API 移出 MVP。
   - 规划档案、不可变规划事件、手工/口述确认链和独立模拟沙盘已完成。
5. M5 移动端整体移出 MVP；共享移动界面和已安装 Rust targets 作为后续起点保留。
