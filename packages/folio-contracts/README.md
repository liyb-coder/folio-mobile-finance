# @folio/contracts

Platform-neutral safety contracts shared by Folio clients and services.

This package intentionally has no React, Tauri, WeChat, storage, network, or model-provider dependency. It defines only the minimum boundary required to keep mini-program input in a reviewable state and confirmed ledger writes explicit, versioned, and idempotent.
