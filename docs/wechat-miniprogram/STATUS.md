# Folio 微信小程序改造状态

更新：2026-08-12

## 目标

以测试先行和对抗式审查方式，把 Folio 的移动体验建设成可审核、可与桌面端共享数据的微信小程序 MVP。

## 已完成

- 完成现有 React/Tauri、语音、文件、加密、Codex CLI 和通知依赖审计；
- 明确小程序定位为日常采集/核对终端，而非桌面端复制品；
- 完成第一性原理技术改造方案；
- 完成测试先行与对抗式审查方案；
- 完成人工堵点清单；
- 决定 M0 不移动现有 `src/` 和 `src-tauri/`，先建立独立共享契约和测试基线。
- 完成 `folio-contracts` 最小包、安全配置、平台端口、提案和确认契约；
- 完成 Taro 4.2.1 官方模板 spike，因 React 19 冲突和依赖安全门禁改用原生微信薄壳；
- 完成无运行依赖的 `apps/wechat-mini` 游客模式壳层和 Canvas 验证页；
- 对抗审查发现并修复 5 类问题，每项均有回归门禁。
- 完成 M1 原生 WXML/JS 只读产品壳：解锁占位、总览、资产、流水、统一采集、提醒、AI 管家和设置；
- 完成无密钥客户端的 DeepSeek 测试 BFF、桌面适配器和精确证据校验；
- 完成 M2a 文字采集链路：无密钥客户端调用 DeepSeek BFF，严格校验证据后渲染待核对卡；
- 微信开发者工具 2.02.2607271（Apple Silicon）已安装并开启 CLI 服务端口；
- 小程序 19 项自动化测试通过，DeepSeek BFF 6 项模拟上游测试通过。

## 当前里程碑

M2a｜文字采集到待核对提案：代码与对抗测试完成。微信开发者工具已安装，但当前 `touristappid` 被新版工具拒绝；需要真实 AppID/扫码登录后才能补编译、视觉和真机证据。DeepSeek 真实联网检查仍等待服务端进程可见的环境凭证。

## 下一步

1. 由主体管理员提供真实 AppID，完成微信开发者工具扫码登录和开发者/体验成员授权；
2. 导入 `apps/wechat-mini`，按 `folio-v2-mobile*.png` 执行同视口对比和修正；
3. 让服务端进程可见 `DEEPSEEK_API_KEY`，执行一次虚构数据 `npm run deepseek:doctor`；
4. 进入 M2b：先写微信身份、提案版本、前台重新验证和重放攻击测试，再开放逐项确认；
5. M2b 稳定后再接语音 ASR 和图片/PDF OCR，不并行扩大权限面。

人工走查按 `MANUAL_WALKTHROUGH_TEST.md` 执行。当前手机不在身边时，可以先完成开发者工具 W01–W10 与对抗项；真机冒烟延后，但不能用模拟器结果替代真机结论。

## 未决人工堵点

见 `HUMAN_BLOCKERS.md`。M0 可不依赖真实 AppID；M1 实机前至少需解决 H01–H03、H11–H12，M2 登录联调前必须再解决 H04–H10。
